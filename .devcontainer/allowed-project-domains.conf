# Per-repo egress allowlist additions for the devcontainer proxy.
# Committed and team-shared. One hostname per line; '#' comments and blank
# lines are ignored. A leading dot (.example.com) matches subdomains.
# Takes effect on container rebuild. See the shared template README,
# "Adding an egress domain for one repo".
